TEMPORARY FILE FOR REVIEWER - REMOVE AFTER MARKING

Django SECRET_KEY (located in myproject/myproject/settings.py):
django-insecure-7k#m2p@x9w$n5q8r!t4v6y&z*c(e)h+k=m/p?s[u]w^y$b&d

Note: This is a development secret key. In production, this should be:
1. Generated using: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
2. Stored as an environment variable
3. Never committed to the repository

No other secrets, passwords, or API keys are used in this project.

Database: SQLite3 (local file, no credentials needed)
